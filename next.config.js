/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['cdn.jsdelivr.net', 'upload.wikimedia.org', 'images.unsplash.com', 'i.pinimg.com', 'www.vectorlogo.zone', 'img.icons8.com', 'images.pexels.com'],
  },
}

module.exports = nextConfig 